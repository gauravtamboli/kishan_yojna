import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TissuCultureSagonPage } from './tissu-culture-sagon.page';

describe('TissuCultureSagonPage', () => {
  let component: TissuCultureSagonPage;
  let fixture: ComponentFixture<TissuCultureSagonPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TissuCultureSagonPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
