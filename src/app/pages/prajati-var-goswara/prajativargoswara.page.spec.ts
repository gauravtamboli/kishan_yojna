import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PrajativargoswaraPage} from './prajativargoswara.page';

describe('PrajativargoswaraPage', () => {
  let component: PrajativargoswaraPage;
  let fixture: ComponentFixture<PrajativargoswaraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PrajativargoswaraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
