import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ClonalNilgiriPage } from './clonal-nilgiri.page';

describe('ClonalNilgiriPage', () => {
  let component: ClonalNilgiriPage;
  let fixture: ComponentFixture<ClonalNilgiriPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ClonalNilgiriPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
