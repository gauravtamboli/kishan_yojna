import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UplabdhiyaPage } from './uplabdhiya.page';

describe('UplabdhiyaPage', () => {
  let component: UplabdhiyaPage;
  let fixture: ComponentFixture<UplabdhiyaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UplabdhiyaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
